# Login Page Redesign: Action Plan

Note: Same approach has been followed for other auth forms also.
This document outlines the steps to redesign the `LoginForm.tsx` component for a more visually appealing and modern user experience using shadcn UI components.

## Action Items

-   [x] **Implement Two-Column Layout:**
    -   [x] Create a new layout structure for the login page.
    -   [x] Dedicate the left column to the login form.
    -   [x] Use the right column for branding elements (image and text).

-   [x] **Enhance Branding:**
    -   [x] Add a placeholder for a branding image in the right column.
    -   [x] Include a welcoming headline and marketing copy below the image.

-   [x] **Modernize Form with Shadcn Components:**
    -   [x] Wrap the entire form in a `Card` component for a structured look.
    -   [x] Restyle the `Input` and `Label` components for email and password for better visual consistency.
    -   [x] Update the primary "Sign In" `Button` with a solid color for emphasis.
    -   [x] Style the "Continue with Google" `Button` as a secondary action.

-   [x] **Improve Readability:**
    -   [x] Adjust typography (font sizes, weights) to create a clear visual hierarchy.
    -   [x] Refine spacing and alignment to reduce clutter.

-   [x] **Ensure Responsive Design:**
    -   [x] Implement media queries to adapt the layout for smaller screens.
    -   [x] Ensure the two-column layout collapses to a single column on mobile devices.
